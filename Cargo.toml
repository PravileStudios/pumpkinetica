[package]
name = "pumpkinetica"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-only"
authors = ["Indrajeeth"]
repository = "https://github.com/PravileStudios/pumpkinetica"

[workspace]
# The IPC example is a standalone crate built separately; keep it out of the
# main plugin build.
exclude = ["examples/ipc-tester"]

[lib]
crate-type = ["cdylib"]

[dependencies]
pumpkin-plugin-api = { git = "https://github.com/Pumpkin-MC/Pumpkin", rev = "5b833274d46b50a519d6b4e532a7b44f05447c64" }
flate2 = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
